version: 0.2

phases:
  install:
    commands:
      - printenv
      - npm install
      - npm install -g eslint
  build:
    commands:
      - echo List current dir...
      - ls -lah
      - echo List codebuild dir
      - ls -lah /codebuild/output/src*/src
      - npm run prod
artifacts:
  files:
    - public/**/*
    - src/**/*
    - package.json
    - webpack.config.js
    - jest.config.js
  discard-paths: yes